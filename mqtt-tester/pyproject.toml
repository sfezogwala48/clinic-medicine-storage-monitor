[project]
name = "mqtt-tester"
version = "0.0.0"
description = "MQTT test client for clinic medicine storage monitor (MicroPython umqtt)"
requires-python = ">=3.10"
dependencies = []

[tool.uv]
package = false
