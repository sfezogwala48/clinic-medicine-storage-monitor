{
  "name": "mqtt-tester",
  "version": "0.0.0",
  "private": true,
  "description": "MQTT tester (Python + MicroPython umqtt, run via uv). See README.md.",
  "type": "module",
  "scripts": {
    "dev": "uv run tester.py burst",
    "burst": "uv run tester.py burst",
    "check": "uv run python -m py_compile tester.py shim.py"
  },
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": "12.3.4",
      "onFail": "download"
    }
  }
}
