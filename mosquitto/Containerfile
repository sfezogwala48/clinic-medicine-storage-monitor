FROM docker.io/eclipse-mosquitto:2
# Railway can't bind-mount ./mosquitto/mosquitto.conf like compose does,
# so bake the config into the image. Build context is the repo root.
COPY mosquitto/mosquitto.conf /mosquitto/config/mosquitto.conf
EXPOSE 1883
